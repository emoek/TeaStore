server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: rabbitmq-logs
    static_configs:
      - targets:
          - localhost
        labels:
          job: rabbitmq-logs
          __path__: /symlogs/sym-kieker-output.dat

    pipeline_stages:
    - regex:
        expression: '^\$0;(\d+);(.*?);(.*?);(\d+);(\d+);(\d+);(.*?);(\d+);(\d+)$'

    - labels:
        timestamp: "$1"
        method: "$2"
        session_id: "$3"
        some_id: "$4"
        another_timestamp: "$5"
        yet_another_timestamp: "$6"
        pod_name: "$7"
        pod_id: "$8"
        some_field: "$9"

    - timestamp:
        source: timestamp
        format: UnixNs

    - output:
        source: message

